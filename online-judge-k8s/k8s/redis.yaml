apiVersion: apps/v1
kind: Deployment
metadata:
  name: online-judge-redis
  labels:
    app: online-judge-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: online-judge-redis
  template:
    metadata:
      labels:
        app: online-judge-redis
    spec:
      containers:
      - name: redis
        image: redis:7
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: online-judge-redis
spec:
  selector:
    app: online-judge-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379